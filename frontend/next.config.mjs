/** @type {import('next').NextConfig} */
const nextConfig = {
  // Standard Next.js SSR/CSR mode for dynamic routes and API calls
  // Use `output: "standalone"` for Docker deployment
};
export default nextConfig;